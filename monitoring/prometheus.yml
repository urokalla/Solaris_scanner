global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:9090"]

  # Postgres metrics
  - job_name: "postgres"
    static_configs:
      - targets: ["postgres_exporter:9187"]

  # Container metrics (optional; requires cadvisor service)
  - job_name: "cadvisor"
    static_configs:
      - targets: ["cadvisor:8080"]

